# Symfony

Use Symfony's trusted proxy configuration and pass the Symfony request through the Symfony request context factory. Keep the framework trust proxy settings and fingerprint trust proxy settings aligned.