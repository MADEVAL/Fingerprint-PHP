# Laravel

Use Laravel trusted proxy middleware and pass the Illuminate request through the Laravel request context factory. Publish application configuration with your secret, privacy mode, trusted proxies, and cookie allowlist.