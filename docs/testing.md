# Testing

Run the local checks:

```bash
composer test
composer analyse
composer cs
```

The test suite covers normalization, trust proxy handling, privacy profiles, secret redaction, matching, and golden fingerprint fixtures.