# Algorithm Versioning

The default algorithm version is `gsfp-v1`.

Patch releases must not change output for the same algorithm version. If canonical payload structure, default signal inclusion, normalization, or hashing changes, a new algorithm version is required.