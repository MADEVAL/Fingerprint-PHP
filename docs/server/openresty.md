# OpenResty

OpenResty can expose richer edge-derived signals through trusted headers from Lua or Nginx variables. JA3/JA4-like values must come from trusted infrastructure and should never be accepted from arbitrary clients.