# LiteSpeed

LiteSpeed and OpenLiteSpeed expose PHP request data through server variables similar to Apache-compatible deployments. Header order is not a stable fingerprint signal. Review proxy and real IP settings before trusting forwarded headers.